---
title: "快速上手"
description: "Oparic 快速上手：从数据到 Origin"
weight: 1
---

> Oparic 的工作方式：**先「提取」，再「分析/出图」**。两步都是卡片，用文件夹串起来。

## 核心模型：两步走

```mermaid
flowchart LR
    A["原始仪器数据<br/>（仪器导出的 .txt 等）"] -->|输入| B["第一步：提取卡片<br/>按仪器格式选择<br/>例：CV 提取: 辰华仪器"]
    B -->|输出文件夹<br/>规范化的 .xlsx| C["第二步：分析 / 绘图卡片<br/>以提取结果目录/文件<br />作为输入<br/>例：绘制最后一圈"]
    C --> D["Origin / Excel / 图片"]
```

- **第一步（提取）**：按你的**仪器型号**选一张「××提取」卡片（如「CV 提取: 辰华仪器」「CV 提取: DIGISIM」），输入是仪器导出的原始数据文件夹，输出是规范化的 `.xlsx`（落到 `<输入>-cv-extract` 这种同级目录）。
- **第二步（分析/绘图）**：选一张分析或绘图卡片（如「计算库伦效率」「绘制最后一圈」「b 值分析」），它的输入就是**第一步生成的那个输出文件夹**。

> 卡片上的“需要先完成”写明了它需要哪张提取卡片先跑过，“相关分析” 是跑完之后推荐的下一张卡片，可点击切换。

## 准备

1. 安装并启动 Oparic（Windows）。
2. 注册/登录。
3. **Origin（可选，但推荐）**：仅 Windows，要求 **Origin ≥ 2021**。装好后状态栏会显示 Origin 已就绪；没装或版本不够也能跑，只是不会自动出 Origin 图。

## 跑通一遍（以 CV 为例）

1. **第一步 · 提取**
   - 画廊 → `CV` → 「CV 提取: 辰华仪器」（或对应你仪器的提取卡片）。
   - 在文件夹选择器里选中**装有 `.txt` 原始文件的文件夹**（不是单个文件）。
   - 自动得到 `<输入文件夹>-cv-extract`，里面是规范化的 `.xlsx`。
2. **第二步 · 分析/绘图**
   - 返回画廊 → 选一张 CV 分析/绘图卡片（如「绘制最后一圈」）。
   - 在文件夹选择器里选**第一步那个 `-cv-extract` 输出文件夹**。
   - 自动绘图；若 Origin ≥ 2021 已就绪，会同步在 Origin 中建表、出图，并保存 `.opju` 到输出文件夹，可在 Origin 内继续精修。

## 常见问题

- **没调起 Origin？** 状态栏会标明原因，Excel 与预览图应该会正常生成，还不能解决的话可以联系我们技术支持！
- **会覆盖我已有的绘图？** 是的。如果此前在 Origin 已经有过绘图**软件会覆盖过去的绘图**，请务必注意在启动前保存工作！我们会弹出提示警告这种状况。
- **「没有成功处理任何 txt」**：确认选的是**文件夹**，且其中是**对应仪器格式**的原始文件（选错提取卡片会导致格式不匹配）。
- **第二步选错文件夹**：分析/绘图卡片读的是**提取后的 `.xlsx/.csv`**，不要再选原始数据文件夹，那些格式无法被直接识别。
